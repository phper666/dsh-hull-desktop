# 规则索引

> 登记表：编号 / 规则摘要 / 所属模块 / 共识文档版本 / 状态
> 编号从 CON-R001 起；来源标注（PRD = M1 PRD 2026-08-14）

| 编号 | 规则摘要 | 所属模块 | 共识版本 | 状态 |
|:-----|:---------|:---------|:---------|:-----|
| CON-R001 | 纯壳原则：永不 fork/patch/替换 dsh 及官方 Web UI，官方每个版本发布当天即可用 | 架构 | — | 生效 |
| CON-R002 | 数据官方化：会话/设置/凭据在 DSH_HOME（默认 ~/.dsh），壳绝不重新实现或改写 | 数据 | — | 生效 |
| CON-R003 | 双升级通道：dsh 升级（壳内 npm overlay）与壳自更新（electron-updater）互不阻塞 | 升级 | — | 生效 |
| CON-R004 | 增量层原则：壳原生功能住 Electron 主进程；需要跑在 dsh 内部的功能必须走官方扩展点（--patch / dsh plugin add / dsh.client） | 架构 | — | 生效 |
| CON-R005 | 升级原子性：staging → 原子替换 → 就绪验证 → 失败自动回滚；绝不原地修改运行中的副本 | 升级 | — | 生效 |
| CON-R006 | 平台范围：M1 仅 macOS Apple Silicon；代码保持跨平台友好 | 平台 | — | 生效 |
| CON-R007 | Node 运行时：捆绑独立 Node（^22.19 \|\| >=24），不依赖用户环境，不采用 ELECTRON_RUN_AS_NODE | 运行时 | — | 生效 |
| CON-R008 | dsh 版本策略：默认 @latest；设置页"指定版本"入口（手输/列表选择，不写死） | 升级 | 共识 v1.1 | 生效 |
| CON-R009 | 升级触发：设置页可配置——手动「检查更新」按钮 + 自动检查开关（默认开）；应用前必须用户确认；「稍后再说」当日不再提示 | UX | 共识 v1.1 | 生效 |
| CON-R010 | 用户数据保持：壳升级/回滚/卸载均不触碰 DSH_HOME 内容 | 数据 | — | 生效 |
| CON-R011 | ~~升级会话保护~~ 已删除：确认后立即升级，会话中断可接受 | 升级 | Q-003 | 已下线 |
| CON-R012 | 双通道互斥：dsh 升级与 Hull 升级同一时间只允许一个排队 | 升级 | 共识 v1.0 | 生效 |
| CON-R013 | registry 通用：支持任意 npm registry（官方/镜像/私有源） | 配置 | 共识 v1.0 | 生效 |
| CON-R014 | 升级原子性补充：启动残留检测恢复（staging 续替 / previous 回滚） | 升级 | 共识 v1.1 | 生效 |
| CON-R015 | 就绪判定：就绪行 + HTTP 探测；探测目标/超时可注入 | 运行时 | 共识 v1.1 | 生效 |
| CON-R016 | 首装可取消 → "未安装"引导态 + 安装按钮；不提供永久跳过 | UX | 共识 v1.1 | 生效 |
| CON-R017 | 看板数据归属：userData/kanban/boards.json，JSON 单文件 + schema version + 原子写，不碰 DSH_HOME | 看板 | 共识 v1.0 | 生效 |
| CON-R018 | 执行模式：manual/auto；auto 必填 AC 四字段（what/expected/verify 强校验 + context 可选） | 看板 | 共识 v1.0 | 生效 |
| CON-R019 | 执行通道：ExecutionProvider 抽象 + ACP 默认实现（JSON-RPC stdio/newSession+prompt/session/cancel/仅已提交文本）；插件备选；CLI 兜底 | 看板 | 共识 v1.0 | 生效 |
| CON-R020 | 人工拖拽最高优先级：直接生效、不自动矫正、冲突确认可强制通过、拖拽写 timeline | 看板 | 共识 v1.0 | 生效 |
| CON-R021 | 执行中修订 AC：编辑 AC=终止 ACP 进程、interrupted、partial 标废弃、AC diff 留痕、验证以最新 AC 为准、interrupted 收敛两选一（不支持"继续原执行"） | 看板 | 共识 v1.2 | 生效 |
| CON-R022 | 父任务聚合：全 Done→父 Done / 任一 Blocked→父 Blocked / 否则 order 最大列；父卡默认聚合推导、人工移动可覆盖 | 看板 | 共识 v1.0 | 生效 |
| CON-R023 | 并行执行进 M2（O-9 变更）：maxParallelTasks 默认 3 可配置；dependencies 为空才可并行；依赖判据/失败传播/死锁兜底见共识 §13 | 看板 | 共识 v1.2 | 生效 |
| CON-R024 | 附件上限：maxAttachmentSizeMB 默认 10 可配置；删卡级联清理评论+附件 | 看板 | 共识 v1.0 | 生效 |
| CON-R025 | 评论来源区分：source{type,agentId,provider}，预留多 agent 平台 | 看板 | 共识 v1.0 | 生效 |
| CON-R026 | 子任务跨列：独立显示 + 父引用徽标 + 父卡跨列聚合 | 看板 | 共识 v1.0 | 生效 |
| CON-R027 | 执行顺序：父任务子任务默认串行；无依赖子任务可并行（并行进 M2，O-9 变更） | 看板 | 共识 v1.2 | 生效 |
| CON-R028 | 干预后仍走 Verify 把关：不绕过人工把关 | 看板 | 共识 v1.0 | 生效 |
| CON-R029 | 执行结果回写：auto 验证通过→Verify；manual 结果手动放入；执行完成列由人工指定时不自动推进 | 看板 | 共识 v1.0 | 生效 |
| CON-R030 | 多 agent 平台派发：agentSpec.subagentPolicy 'auto'（默认，允许跨平台子 agent）/ 'restricted'（仅 dsh 自身）；ExecutionProvider 可扩展多平台，dsh 作 ACP client 可编排跨平台子 agent | 看板 | 共识 v1.1 | 生效 |
| CON-R031 | 多项目看板（决策 1）：boards[] 顶层，每 Board 独立列配置/任务，可创建/切换；M2 默认单看板起步 | 看板 | 共识 v1.3 | 生效 |
| CON-R032 | 执行活动心跳超时（Q-026）：连续 maxExecutionIdleMinutes（默认 30）无活动事件 → 疑似卡死 → failed；非总时长限制 | 看板 | 共识 v1.3 | 生效 |
| CON-R033 | 归档与删除规则（决策 2/P1-E）：Done 可归档（archivedAt/archivedFromColumnId）；归档区可恢复/彻底删除；删看板有 ticket→拒删、无→可删、最后一个→不可删（可改名）；删项目前置清空 ticket | 看板 | 共识 v1.4 | 生效 |
| CON-R-skills-001 | Agent→目录注册表硬编码：claude-code=`~/.claude/skills/`、opencode=`~/.config/opencode/skills/`（同时读 `~/.claude` 与 `~/.agents`）、codex=`~/.codex/skills/`、gemini-cli=`~/.gemini/skills/`、cursor=`~/.cursor/skills/`、windsurf=`~/.codeium/windsurf/skills/`、warp=`~/.warp/skills/`、trae=`~/.trae/skills/`+`~/.trae-cn/skills/`、cline=`~/.cline/skills/`、roo=`~/.roo/skills/`、continue=`~/.continue/skills/`、devin=`~/.config/devin/skills/`、dsh=`~/.dsh/skills/`、harness=`~/.harness/skills/`、qoder=`~/.qoder/skills/`+`~/.qoder-cn/skills/`、reasonix=`~/.reasonix/skills/`、shared=`~/.agents/skills/`；单点集中维护；无标准约定的 agent（Amazon Q/Aider）不纳入 | Skills检查器 | 共识 v1.6 | 生效 |
| CON-R-skills-002 | 全局 vs 平台 scoped 判定 = 目录位置 + symlink realpath 解析：`~/.agents`=universal、`~/.claude`=claude+opencode、`~/.config/opencode`=opencode 专属、其余平台目录=各自专属、trae/trae-cn 归并 trae、qoder/qoder-cn 归并 qoder；按 name 聚合、realpath 同源去重 | Skills检查器 | 共识 v1.6 | 生效 |
| CON-R-skills-003 | 破坏性操作（移除/升级/禁用）二次确认（路径+影响平台）；全局 skill 额外警示；移除前备份回收站（`<userData>/skills/trash/`，可恢复）；回收站 TTL 30 天+容量上限 500MB（最旧先删），条目记录原路径+删除时间，恢复冲突提示；操作日志留痕 | Skills检查器 | 共识 v1.2 | 生效 |
| CON-R-skills-004 | 升级检测 = 内容哈希（SHA-256，path+content 排序）对比远端哈希，非版本号；远端哈希来源（Q-034 v1.3 变更，skills-lock.json 移除）：① 各平台 lock .arkcli-managed-skills.json（已实现，标准位置）→ ② frontmatter metadata.source 推断 → ③ cc-switch content_hash（表空待办）→ ④ git remote 临时 clone（网络成本待办），按 name 匹配，均无 unknown；升级执行 npx skills update 优先（生产已接线，不依赖 source URL，失败降级 git-staging 原子替换回滚），git 来源不原位 pull 改 staging clone 原子替换，metadata.source 非 git 用 URL 重取，无 source+无远端哈希 升级禁用 | Skills检查器 | 共识 v1.3 | 生效 |
| CON-R-skills-005 | 来源解析一级（Q-034 v1.3 变更）：frontmatter metadata.source 匹配 ^https:// 采用；均无显示「来源未知」，跳转/升级 git 轨禁用（npx 轨升级仍可用，不依赖 source） | Skills检查器 | 共识 v1.3 | 生效 |
| CON-R-skills-006 | 数据归属壳 userData（`<userData>/skills/`：disabled 目录/回收站/哈希缓存/操作日志），不写 DSH_HOME | Skills检查器 | 共识 v1.1 | 生效 |
| CON-R-skills-007 | 安全校验：openExternal 仅接受 `^https://` 白名单（拒 file:/javascript:/data:）；skill 目录名经 basename(realpath) 校验拒 `../`/空名/非法字符；renderer 仅经 preload 桥访问壳能力 | Skills检查器 | 共识 v1.2 | 生效 |
| CON-R-skills-008 | 禁用/启用 = 移目录真禁用（T-4 定案）：按物理路径粒度独立禁用/启用（共享目录移出=全平台禁，平台副本单独禁）；symlink 来源移除 symlink（源保留 SSOT）、实目录 rename 到 `<userData>/skills/disabled/<skill-name>/`，userData 记录路径映射，启用恢复；agent 平台真生效；不破坏 SKILL.md 内容 | Skills检查器 | 共识 v1.2 | 生效 |
| CON-R-skills-009 | 扫描异步 + 哈希缓存（目录 mtime 变化才重算）；首屏 <2s（200+ skill）；哈希后台线程/分批 | Skills检查器 | 共识 v1.1 | 生效 |
| CON-R-skills-010 | 搜索分本地/远程两场景且分开（T-2 定案）：两 tab（本地/远程）默认本地；本地过滤已扫描列表；远程调 `npx skills find` 展示结果（名称/描述/来源/安装数）仅浏览标注「未安装」，仅来源跳转 | Skills检查器 | 共识 v1.2 | 生效 |
| CON-R-editor-001 | 编辑端用 EasyMDE（create/edit 两处 textarea → EasyMDE，工具栏+预览+快捷键）；本地 vendoring（src/renderer/vendor/）+ shell.html `<script src>`，CSP script-src 'self' 不走 CDN；弹窗关闭 `editor.destroy()`+移除监听、每次打开新建实例不复用（Q-041）；CM5 主题优先 vendor 独立 CSS（`<link>`，style-src 'self'），运行时强制 inline 则仅 style 加 'unsafe-inline'（script 仍 'self'，实测记录）（Q-042）；暗色主题 vendor CM5 暗色 CSS scoped `.EasyMDE`（tokens #1e2430/#2a3342）（Q-045） | 看板 | 共识 v1.2 | 生效 |
| CON-R-editor-002 | 渲染端 markdown-it + DOMPurify 消毒：detail 只读态 markdown-it 渲染 → DOMPurify 消毒 → 插 DOM；凡用户内容进 HTML 的路径统一走 DOMPurify（含 history/邮件等其余只读文本）；覆盖面审计=枚举 kanban.js 全部 innerHTML/insertAdjacentHTML 站点（详情/comment/timeline/附件名/assignee/labels/标题预览）统一 sanitize，清单入实现记录（Q-043） | 看板 | 共识 v1.2 | 生效 |
| CON-R-editor-003 | 数据零迁移：description 保持 string\|null（Markdown 即文本，空存 null），boards.json 结构/schema version 不变，KanbanStore 读写逻辑不变，不碰 DSH_HOME（CON-R002 相容） | 看板 | 共识 v1.0 | 生效 |
| CON-R-editor-004 | markdown-it 锁 v14.1.0：v15 起无 UMD build（构建失效），vendoring 固定版本，不随 @latest 升级 | 看板 | 共识 v1.0 | 生效 |
| CON-R-editor-005 | GFM 插件清单定案（Q-044/U-2）：markdown-it v14 默认（table+strikethrough）+ markdown-it-task-lists（任务列表）；两端对齐 EasyMDE 默认（表格/任务列表/删除线） | 看板 | 共识 v1.2 | 生效 |
| CON-R-editor-006 | comment 框（:226）升级 EasyMDE：create/edit/comment 三处 textarea 均接 EasyMDE（工具栏+预览+快捷键）；评论内容为 Markdown 原样，渲染评论时同样走 markdown-it + DOMPurify 消毒（CON-R-editor-002 适用） | 看板 | 共识 v1.1 | 生效 |
| CON-R-timeline-001 | 时间线视图 = 任务活动流按时间倒序（来源 createdAt/timeline createdAt/execution startedAt·finishedAt），零 schema 变更；execution 缺 startedAt/finishedAt → updatedAt 兜底或跳过标记"时间未知"；同时间戳 tie 按 id 稳定排序（倒序后建在前）（Q-055） | 看板时间线 | 共识 v1.2 | 生效 |
| CON-R-timeline-002 | 日历视图 = 月/周粒度（默认月），任务按 dueDate 落格，startDate+dueDate 区间跨格；过期任务视觉标记；仅 startDate → 显示至当月末尾；日期按本地时区解析（date-only 用 new Date(y,m,d) 避免 UTC 偏移）；月标签中文 Intl.DateTimeFormat('zh-CN')；跨月条带按日遍历渲染（Q-054） | 看板时间线 | 共识 v1.2 | 生效 |
| CON-R-timeline-003 | Task 新增 startDate 字段（ISO 日期字符串，可空，与 dueDate 同型）；createTask/updateTask 以可选参数扩展读写（向后兼容）；详情面板可设/清空并持久化；契约 feishu-b1-m2-kanban-api-contract.md 须同步更新（Task schema + 参数说明）+ preload 类型/IPC 校验同步，走 change-propagation 落地并登记变更摘要（Q-052） | 看板时间线 | 共识 v1.2 | 生效 |
| CON-R-timeline-004 | schema v2 迁移：KANBAN_SCHEMA_VERSION 1→2，migrate() 遍历 boards[]→columns[]→tasks[] 三层补 startDate:null 其余原样保留；幂等（重复执行不重复加/不报错）；失败沿用 M2 兜底（备份 boards.json.corrupt-<ts> → 重建默认看板）（Q-051） | 看板时间线 | 共识 v1.2 | 生效 |
| CON-R-timeline-005 | 视图接入复用 kanban.js viewNames 状态机 + render 函数 + 通用工具栏，不新建页面/入口；视图持久化 = renderer localStorage（key=kanban:lastView）重启保持，不 bump HullSettings schema；IPC/存储层零新增（仅 Task 字段扩展）（Q-053） | 看板时间线 | 共识 v1.2 | 生效 |
| CON-R-timeline-006 | 性能：任务量大（≥1000 卡）时时间线/日历按需渲染（分页/按月切片），不一次渲染全量 DOM；视图切换响应 < 300ms | 看板时间线 | 共识 v1.1 | 生效 |
| CON-R-timeline-007 | 边界容错：startDate>dueDate 以 dueDate 为准单日显示（可加非阻塞 UI 提示）；startDate/dueDate 非法日期串丢弃置 null 不阻塞加载 | 看板时间线 | 共识 v1.1 | 生效 |
| CON-R-theme-001 | 主题范围 = 仅壳 UI（nav/看板/Skills/设置/编辑器/时间线），官方 dsh Web UI 零注入（CON-R001 不破） | 壳 | 共识 v1.0 | 生效 |
| CON-R-theme-002 | 主题载体 = CSS 变量（--hull-*）+ 壳根节点 data-theme="dark\|light" 属性切换 | 壳 | 共识 v1.0 | 生效 |
| CON-R-theme-003 | 主题持久化 = settings.json（HullSettings.theme 字段），字段级扩展不 bump schemaVersion（保持 3，对齐 S6 registry 先例；旧数据/非法值读时回退 dark） | 壳 | 共识 v1.0 | 生效 |
| CON-R-theme-004 | 默认主题 = dark（保持现状），旧数据/非法值回退 dark 并归一化 | 壳 | 共识 v1.0 | 生效 |
| CON-R-theme-005 | 硬编码色值全部抽取为 CSS 变量（--hull-*），无残留（除变量定义处）；亮/暗两套变量集 | 壳 | 共识 v1.0 | 生效 |
| CON-R-theme-006 | 主题跟随系统：theme 枚举扩展 'system'（默认仍 dark），main nativeTheme.themeSource 透传 + renderer matchMedia 解析，持久化不 bump schemaVersion | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-001 | 两包管理器支持（npm/pnpm），默认 pnpm，设置页选择 + settings 持久化 | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-002 | pnpm 装 dsh 用 prefer-symlinked-executables=true（POSIX .bin 变 symlink） | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-003 | 原生依赖（koffi/node-pty 等）自动 rebuild，失败告警不阻断 | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-004 | spawn 不依赖 .bin shim，解析包真实 JS 入口（createRequire + bin 字段） | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-005 | spawn 用 process.execPath + ELECTRON_RUN_AS_NODE=1，剥离 NODE_OPTIONS/ELECTRON_* | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-006 | 两包管理器取消安装（杀子进程）语义一致 | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-007 | 错误码映射两包管理器一致（registry-unreachable/npm-install-failed 复用） | 壳 | 共识 v1.2 | 生效 |
| CON-R-pkgmgr-008 | settings.packageManager 非法值回退 pnpm；包管理器不可用回退 npm | 壳 | 共识 v1.2 | 生效 |
| CON-R-packaging-001 | 打包工具 = electron-builder，配置 electron-builder.yml 三平台 target | 壳 | 共识 v1.0 | 生效 |
| CON-R-packaging-002 | macOS = dmg+zip（Apple Silicon）；Windows = nsis+portable；Linux = AppImage+deb | 壳 | 共识 v1.0 | 生效 |
| CON-R-packaging-003 | 三端捆绑 node（fetch-node 机制按平台），dsh 不依赖用户 node | 壳 | 共识 v1.0 | 生效 |
| CON-R-packaging-004 | 自动更新 = electron-updater 延续，三端发同一更新源 | 壳 | 共识 v1.0 | 生效 |
| CON-R-packaging-005 | **mac 用自签名代码签名证书**（「Dsh Hull Code Signing」，2026-08-28 二次修订：ad-hoc 签名 DR 钉死 cdhash 跨版本必失败不可用；证书 DR=指纹可移植，同证书跨版本 Squirrel 校验通过；CI 走 CSC_LINK/CSC_KEY_PASSWORD secrets，所有版本必须同证书；公证仍不做） | 壳 | 共识 v1.3 | 生效 |
| CON-R-packaging-006 | CON-R006 平台范围扩展：macOS → 三端 | 壳 | 共识 v1.0 | 生效 |
| CON-R-packaging-007 | 单平台打包失败不阻塞其他平台 | 壳 | 共识 v1.0 | 生效 |
| CON-R-packaging-008 | 捆绑 node 失败回退用户 node（PATH 兜底） | 壳 | 共识 v1.0 | 生效 |
