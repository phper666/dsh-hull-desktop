# 影响清单（主窗口形态变更）

> 变更源：2026-08-18 用户产品决策（共识 v1.5）
> 变更摘要：见 docs/spec/变更摘要-Hull.md

| 受影响产物 | 路径 | 处理方式 | 责任人 | 状态 |
|:-----------|:-----|:---------|:-------|:-----|
| S1 契约（主窗口/loadURL 接口） | docs/api/feishu-s1-api-contract.md | 已更新（主窗口 = 壳框架 + 右侧 WebContentsView 内嵌；loadURL 语义迁移；占位页位置；托盘补充定位） | phper666 | 已闭环（v0.3） |
| S1 设计（窗口/loadURL/preload 方案） | docs/design/S1-壳骨架-design.md | 已产出方案（S8 主窗口形态，含壳框架 + WebContentsView；偏离 D6 注记同步） | phper666 | 已闭环（S8 frozen + 实现完成） |
| S6 契约（设置入口） | docs/api/feishu-s6-api-contract.md | 已更新（双入口注记：壳导航为主、托盘补充；partition 'settings' 隔离语义保留，实现表述归设计） | phper666 | 已闭环（v0.3） |
| S6 设计（设置窗口 partition） | docs/design/S6-设置页托盘-design.md | 已复核（壳导航内入口 + partition 隔离表述） | phper666 | 已闭环（入口双轨注记） |
| preload/partition 方案（S1 偏离注记） | src/window/WindowManager.ts 注释 + S1 设计 | 已实现（WebContentsView 与壳 UI session 隔离） | phper666 | 已闭环（partition 'shell' + 官方默认 session） |
| S2 契约（未安装引导态位置） | docs/api/feishu-s2-api-contract.md | 已更新（引导态位置表述同步：壳框架右侧内容区，3 处） | phper666 | 已闭环（v0.4） |
| S2 设计（引导态表述） | docs/design/S2-dsh运行时-design.md | 已复核（同上） | phper666 | 已闭环（S8 实现同步） |
| S7 契约/设计（E2E-06 主窗口场景） | docs/api/feishu-s7-api-contract.md + docs/design/S7-测试验收-design.md | 契约已更新（E2E-01/06 注记，断言更新归实现波）；设计已复核 | phper666 | 已闭环（契约 v0.3 + e2e 已适配 8/8） |
| 托盘（保留补充入口） | src/tray/TrayController.ts | 无需变更（保留） | phper666 | 已闭环（保留确认） |
| 测试（e2e 冷启动/托盘场景） | tests/e2e/cold-start.spec.ts + helpers.ts | 已更新（窗口结构断言适配，helpers URL 定位） | phper666 | 已闭环（已适配 8/8） |

## 引用点验证（grep，2026-08-18）

逐文件确认"loadURL / 主窗口 / 设置窗口"引用点存在（下文行号为 grep 时行号）：

- **docs/api/feishu-s1-api-contract.md**：L28 主窗口 loadURL 官方 UI（零注入，CON-R001）；L45 就绪行出现 → HTTP 探测 200 → 主窗口 loadURL → 可用；L94 语义固化 loadURL 恒用就绪行 URL；L20/L30 托盘基础（打开主窗口/设置入口/退出）
- **docs/design/S1-壳骨架-design.md**：L21 主窗口 loadURL 官方 UI；L94~L96 preload 零注入 + 偏离注记（registerPreloadScript，2026-08-18）；L126 WindowManager 主窗口创建/占位页/loadURL；L271 运行时 loadURL 官方地址；L293 探测语义
- **docs/api/feishu-s6-api-contract.md**：L31 托盘完整；L32 关闭主窗口默认隐藏；L93 常规：dsh 运行中、打开主窗口、Hull 设置、检查 dsh 更新、退出
- **docs/design/S6-设置页托盘-design.md**：L18 范围（设置页独立壳窗口）；L41 B1 独立 partition（不继承主窗口 session.setPreloads）；L99/L133/L185/L202 设置窗口生命周期/单实例
- **src/window/WindowManager.ts**：L35~L36 偏离 D6 注记（electron loadURL 无 preload 选项 → registerPreloadScript）；L42 ready → loadURL 官方地址；L110/L120/L139 loadURL 调用（官方地址 + 占位页）
- **docs/api/feishu-s2-api-contract.md**：L28 未安装引导态（主窗口显示安装按钮）；L45 取消 → 引导态；L127 取消后主窗口未安装引导态
- **docs/design/S2-dsh运行时-design.md**：L22 取消安装 → 未安装引导态（主窗口显示安装按钮）
- **docs/api/feishu-s7-api-contract.md**：L80 E2E-06 托盘（打开主窗口/设置入口/升级中禁用）
- **docs/design/S7-测试验收-design.md**：L149 E2E-06 托盘（打开主窗口/设置入口/升级中禁用）
- **src/tray/TrayController.ts**：托盘实现（打开主窗口/设置/退出）——保留补充入口，无需变更
- **tests/e2e/cold-start.spec.ts**：L28 启动 → 主窗口出现 → UI 可交互 ≤10s；L89~L102 托盘/打开主窗口/隐藏到托盘
- **tests/e2e/helpers.ts**：L78~L97 主窗口定位（按 URL 排除 settings.html）；L142~L153 等待主窗口 page 出现（窗口创建顺序固定）

> 未发现引用点：feishu-s3/s4/s5 契约与对应设计无主窗口/loadURL 表述（grep 验证），不在清单内。

## 闭环声明

全部影响项已闭环，2026-08-18。
