# 变更摘要-M2看板（L2 模块详情）

> M2 看板模块变更详情。每条 ≤200 字，delta-only、编号驱动、取代链、反哺 Q-items。最新在前。
> L1 索引：docs/spec/变更摘要.md · 共识：docs/spec/共识-Hull桌面壳-M2看板.md · 规则索引：docs/spec/规则索引.md

## 2026-09-03 看板真实场景测试 4 缺陷修复（createColumn 原语 / Blocked 落列 / ✓ 刷新 / 视图记忆持久分区）

- 类型：缺陷修复（真实 UI 场景测试驱动：Playwright Electron 全真实壳 31 场景 + boards.json 双向核对）
- 内容：① **新建列 100% 失败**（renderer 误走 updateColumn(null) 必报「列不存在」）→ 新增 store.createColumn 原语 + `kanban:createColumn` channel（白名单 18→19、共面 44→45）+ preload 桥，UI 改调新原语，建/改/隐/删+卡片迁 Todo 全生命周期打通；② **卡片永不进 Blocked 列**（moveTask blocked 分支漏改 columnId）→ 补 `task.columnId = toColumnId`，出 Blocked 回来源列设计语义（P2-4）不变；③ **「✓ 确认完成」后 UI 不刷新**（数据已落 Done）→ data-verify 成功路径补 loadBoard；④ **视图记忆（Q-053）跨重启丢失** → 壳页 partition 'shell'→'persist:shell'（localStorage 落 userData/Partitions/shell）
- 影响：shared/ipc-channels + KanbanIpc 双白名单同步；ipc-channels.test 计数断言更新；KanbanStore.test K16/K17 补 columnId 断言 + createColumn 生命周期测试；新增 tests/e2e/kanban-bugfix.spec.ts 4 回归用例；单测 852 绿；e2e 看板 3 spec + settings/cold-start 全绿；WindowManager D3 注释同步（「壳页无持久数据」前提随 Q-053 失效）；契约传播与影响清单见 docs/spec/影响清单-BUG1-createColumn.md；留痕 docs/records/KB-真实场景4缺陷-kanban-record.md
- 状态：已修复并验证（2026-09-03，场景脚本 31/31）

## 2026-09-02 U-003 依赖图可视化承接完成（ticket t100108）

- 类型：后续规划项提升实现（共识 §15.3/PRD §12 U-003，判级复杂）
- 内容：**并行依赖图可视化交付**——①父卡详情摘要入口条（依赖数/池 x/3/八态实时计数）→ 独立弹框 DAG（近全屏+滚轮缩放/拖拽平移/⛶适配/⤢全屏/自定义倍率；八态着色+fail-fast 中止链+并行池水位+左栏子任务列表联动）；②**编辑表单「前置依赖」多选**（同父兄弟、反向边防环守卫——store 无环检测，UI 层补守卫）补齐依赖声明 UI 缺口
- 影响：U-003 closed（共识 §12/PRD §12 回写）；零 schema 变更、零 IPC 新增（getExecutionSnapshot 复用）；渲染层新模块 depgraph-core.js（纯函数 23 单测，test:unit glob 扩 src/renderer/*.test.js）+ depgraph.js/.css；记录 docs/records/U3-依赖图-kanban-depgraph-record.md；lesson 2026-09-02-graph-ui-cycle-guard
- 状态：已合并 main（merge 5cd25a9，五轮用户验收通过）

## 2026-08-22 看板编辑器扫描 Q-041~Q-047 回写（共识 v1.2）

- 类型：共识修订回写（三角色扫描结论 + PM 决策，solo 上下文）
- 内容：**Q-041~Q-047 全部 closed**——① Q-041 EasyMDE 实例生命周期：弹窗关闭 `editor.destroy()`+移除监听、每次打开新建实例不复用（防泄漏），泄漏检测进 QA；② Q-042 CSP/CodeMirror 兼容：实现前验证，CM5 主题优先 vendor 独立 CSS 文件（`<link>`，style-src 'self'），运行时强制 inline 则仅 style 加 'unsafe-inline'（script 仍 'self'）实测记录；③ Q-043 DOMPurify 覆盖面：枚举 kanban.js 全部 innerHTML/insertAdjacentHTML 站点统一 sanitize + 审计清单入实现记录；④ Q-044 GFM 插件清单定案：markdown-it v14 默认（table+strikethrough）+ markdown-it-task-lists，**U-2 一并关闭**；⑤ Q-045 暗色主题：vendor CM5 暗色 CSS scoped `.EasyMDE`（tokens #1e2430/#2a3342）+ markdown-it 渲染样式补 kanban.css；⑥ Q-046 弹窗焦点/ESC：初始化 focus、ESC modal 层优先（编辑器不拦截）、Tab 工具栏→编辑器→按钮；⑦ Q-047 预览/渲染一致性：e2e 比对 EasyMDE preview vs markdown-it+GFM 结构语义一致，视觉差异不阻断
- 影响：共识 §1/§8（CON-R-editor-001/002 备注补充、005 定案生效）/§10/§11（新增 §11.1 扫描回写表）/§12/§13/§14.1 E1 验收⑧/§15.2；规则索引 CON-R-editor-001/002/005 更新（共识 v1.2）
- 状态：已回写（共识 v1.2 已发布，2026-08-22）

## 2026-08-22 看板时间线扫描回写 Q-051~Q-055 全定案（共识 v1.2）

- 类型：共识修订回写（三角色扫描 Q-items 确认 + PM 决策 solo 上下文）
- 内容：**Q-051~Q-055 全部关闭**——① Q-051 迁移嵌套遍历：migrate() 遍历 boards[]→columns[]→tasks[] 三层补 startDate:null，幂等（重复执行不重复加/不报错），version 升 2，失败走 M2 兜底；② Q-052 startDate 契约变更传播：createTask/updateTask 可选参数扩展（向后兼容），契约 feishu-b1-m2-kanban-api-contract.md 须同步更新（Task schema + 参数说明）+ preload 类型/IPC 校验同步，走 change-propagation 落地（本条即登记）；③ Q-053 视图持久化 localStorage（key=kanban:lastView），不 bump HullSettings schema、无 IPC 新增；④ Q-054 日历 CJK/时区：本地时区解析（new Date(y,m,d)）+ 中文月标签 Intl.DateTimeFormat('zh-CN') + 跨月条带按日遍历；⑤ Q-055 时间线缺失兜底：updatedAt 兜底或跳过标记"时间未知"，同时间戳按 id 稳定排序
- 影响：CON-R-timeline-001~005 修订生效（规则索引已登记，共识版本 v1.2）；§5.1/§5.2/§7.1/§7.2/§9/§12/§13 同步；SemVer 规则注记 §15.2（后续破坏/数据迁移级变更 → v2.0）；Q-051~Q-055 closed
- 状态：已回写（共识 v1.2，2026-08-22）

## 2026-08-22 看板时间线/日历共识发布 v1.1（U-1~U-5 全定案）

- 类型：共识基线发布（M2 后续规划项提升承接）
- 内容：时间线/日历视图共识发布（原 M2 §15.3 后续规划项提升）。**U-1~U-5 全定案**：① 日历粒度=月视图+周视图（默认月）；② startDate 可空（仅日历规划场景填）；③ 不新增 duration/endDate（区间=startDate→dueDate，YAGNI）；④ 不做拖拽改期（本期）；⑤ 时间线/日历为两个独立视图
- 影响：登记 CON-R-timeline-001~007（时间线视图活动流零 schema 变更 / 日历视图 dueDate 落格+区间跨格 / Task 新增 startDate / schema v1→v2 迁移 / 视图接入复用 viewNames / 性能按需渲染 / 边界容错）；PRD 升 v0.2；五视图（Board/List/Archive/Timeline/Calendar）
- 状态：已发布（2026-08-22，用户确认 + U-1~U-5 定案）

## 2026-08-22 看板编辑器 U-1 定案：comment 框升级 EasyMDE（共识 v1.1）

- 类型：共识定案回写（用户决策应用 + 发布）
- 内容：**U-1 关闭**——comment 框（kanban.js:226）也升级 EasyMDE，推翻 v1.0 默认"保持 textarea"建议；CON-R-editor-006 由"保持 textarea（变更中）"→"comment 升级 EasyMDE（生效，定案）"；create/edit/comment 三处统一 EasyMDE（工具栏+预览+快捷键），评论内容 Markdown 原样，渲染评论时同样走 markdown-it + DOMPurify 消毒（CON-R-editor-002 适用）；回写共识 §2/§5.1/§6/§8/§9/§12/§14.1 E1/§15.2/§15.3、规则索引 CON-R-editor-006、PRD v0.2（§3.1/FR-1/FR-2/FR-4/§6 T1/§7）
- 影响：看板编辑器共识 v1.0 草稿 → v1.1 已发布；E1 子需求验收 ⑦ 明确 comment 升级 + 评论渲染消毒
- 状态：已发布（2026-08-22，用户确认内容 + 决策应用）

## 2026-08-22 看板编辑器共识基线发布（共识 v1.0 草稿）

- 类型：共识基线建立（编辑器增量）
- 内容：看板 ticket 内容编辑器升级首次建立——编辑端（create/edit）textarea → EasyMDE（工具栏+预览+快捷键）；渲染端（detail 只读）escape 纯文本 → markdown-it 渲染 + DOMPurify 消毒；三库本地 vendoring（CSP script-src 'self' 不走 CDN）；数据零迁移（description 保持 string\|null，boards.json 结构不变）；登记规则 CON-R-editor-001~006；未决项 U-1~U-3（对应 PRD T1~T3，U-1 后续定案升级 comment）
- 数据来源：PRD v0.1（docs/prd/2026-08-22-kanban-editor-prd.md）
- 状态：已发布（2026-08-22，用户确认 + U-1 定案，见上条）

## 2026-08-21 M2 review 修复落地

- 类型：实现修复（战略 review 后修订）
- 内容：B5 🔴-1 修复（currentExecutionId 校验宽松化 + 6 round-trip 测试覆盖真实 B3 产物 + B3 markSucceeded 补 execution timeline id 对齐）；B3 🔴-2 修复（ExecutionEngine 执行态直写全链路加 flushSync）；B3/B4 🟡-1 修复（VerifyGate manual 分支明确返回 succeeded）；B4 🟡-3 修复（ApprovalManager/ExecutionEngine 改 EventEmitter + ExecIpc 推送链）；B3 🟡-2 双 kill 竞态 + B3 🟡-4 succeeded 清 currentExecutionId + B4 ACPProvider→ApprovalManager 生产路由
- 影响：M2 全 4 处 🔴 + 🟡 问题全部修复，B1/B2/B3/B4/B5 验收通过
- 状态：已修复（2026-08-21）

## 2026-08-21 M2 验收收尾标记

- 类型：里程碑完成
- 内容：M2 五子需求（B1 数据模型/B2 UI/B3 执行引擎/B4 执行集成/B5 导出导入）全部实现 + 战略 review + 修复闭环 + 交付核验通过
- 影响：M2 可收，docs/records/M2-交付核验.md 为核验事实源
- 状态：M2 通过（460 单测 + 8 集成全绿）

## 2026-08-20 B1 契约冻结（feishu-b1-m2-kanban-api-contract.md）

- 类型：契约冻结（两轮复核闭环 + 用户裁决放行）
- 内容：B1 看板数据模型与持久化契约冻结——完整 JSON Schema（含归档 archivedAt/archivedFromColumnId）+ 16 IPC 原语（含 deleteColumn/archive/restore/purge）+ 7 错误码 + 31 测试场景；对齐 CON-R033 删除规则；B1 store 内置执行态守卫
- 影响：B1 契约可作为 B2/B3 实现依据；B3（复杂）契约冻结后须先出技术方案再实现
- 状态：已冻结（2026-08-20）

## 2026-08-21 M2 五契约全部冻结（B1-B5）

- 类型：契约冻结（ora-1 多轮复核闭环 + 用户裁决）
- 内容：B1（数据模型）→ B2（UI）→ B3（执行引擎）→ B4（执行集成）→ B5（导出分享）五契约全部冻结——B1 16 原语/7 错误码；B3 10 执行控制 IPC + confirmVerify + pause O-11；B4 deadlineAt 主进程审批 + ProviderRegistry；B5 先校验后重映射 + 重申 B1 校验；测试 E/U/A/X 编号齐备
- 影响：M2 契约全部冻结，进入实现管道（B3 复杂先技术方案再实现）
- 状态：已冻结（2026-08-21）

## 2026-08-20 M2 归档机制 + 删除规则 + 视图切换（共识 v1.4）

- 类型：共识修订回写（决策 1/2/3 + B1 复核决策 P1-E）
- 内容：① **归档功能 M2 加**（B1/B2 范围）：Done 可归档 → 归档区只读视图，可恢复（回 Done/原列）/彻底删除；Task 增 `archivedAt` + `archivedFromColumnId`；② **删除规则（P1-E）**：删看板有 ticket（含归档）→拒删、无→可删、最后一个→不可删（可改名）；删项目前置清空 ticket；"删除唯一看板→validation-error" 废弃；③ **视图切换 M2 加**：看板/列表（表格排序筛选）/归档（只读）三视图，同 tasks 数据多视图渲染；时间线/日历视图 M2+；④ 新规则 **CON-R033**（归档与删除）；登记 Q-030 closed
- 影响：§7.1 增 2 字段、§8 增 CON-R033、§12 归档区/视图切换、§2/§15.3 标注；B1 契约复核决策影响 deleteBoard 错误语义（validation-error → 决策 2 规则）
- 状态：已回写（共识 v1.4，2026-08-20）

## 2026-08-20 M2 子需求拆解 B1-B5（Gate B）

- 类型：子需求拆解（Phase 4，Gate B 就绪）
- 内容：M2 任务看板拆 5 子需求 B1~B5——B1 看板数据模型与持久化 / B2 看板 UI 与交互 / B3 执行引擎与状态机 / B4 执行集成与审批 / B5 导出/导入与分享；写入共识 §14.1 子需求清单（编号/验收标准/规则绑定/依赖/来源 PRD/飞书 ticket）；ticket 化 t100101~t100105（飞书 dsh-hull-desktop 清单 Backlog 列，类型 Story，B1-B3=P0 / B4-B5=P1）
- 影响：M2 进入契约阶段（story-to-contract），按 B1→B2→B3→B4→B5 顺序拆契约；B4 依赖 B3，B2/B5 依赖 B1
- 状态：已落载体（共识 §14.1 + 飞书 ticket，2026-08-20）

## 2026-08-20 M2 三角色扫描 4 P2 回写 + 多项目看板提前 + 实时协作记录（共识 v1.3）

- 类型：共识修订回写（Q-026~Q-029 确认 + 2 个产品决策）
- 内容：① **Q-026 修正**：执行超时改"活动心跳超时"（连续 N 分钟无活动事件才 failed，非总时长；maxExecutionIdleMinutes 默认 30 可配置，防误杀超长任务）；② Q-027 Blocked 隐藏列语义（隐藏=不显示，解除后回原列）；③ Q-028 agent 评论只读不可删；④ Q-029 加载 500ms 测量口径（≤5MB 数据集 + 冷热启动）；⑤ **多项目看板提前进 M2**（boards[] 顶层结构，创建/切换看板，各 Board 独立列+任务）；⑥ **实时协作分享记录 M2+**（需服务器+同步，关联 O-4；M2 只做导出分享 FR-16；权限模型 M2+ 引入）
- 影响：§7.1 Board 顶层结构确认；§9 心跳超时常量；§12 多项目看板/空态/隐藏列；§13 心跳超时；§7.4 agent 评论只读；Q-026~Q-029 closed
- 状态：已回写（共识 v1.3，2026-08-20）

## 2026-08-20 M2 三角色扫描 10 P1 回写 + 并行进 M2（共识 v1.2）

- 类型：共识修订回写（Q-016~Q-025 确认 + O-9 变更 + maxParallelTasks=3）
- 内容：① **O-9 变更**：并行提前进 M2（原 PRD P2 增强）；② **CON-R023 修改**：maxParallelTasks 默认 5→3；③ Q-016 并行闭环（依赖判据 succeeded/manual 按列 Done、失败传播、死锁兜底、父卡派生态）；④ Q-017 壳重启执行态收敛；⑤ Q-018 permission_request 审批流（30s 超时 deny/FIFO/写 timeline）；⑥ Q-019 级联删除补全（executions log/执行态禁删/摘除队列/清依赖引用）；⑦ Q-020 §7.1 增 assignee；⑧ Q-021 空态三态；⑨ Q-022 interrupted 修订（删"继续原执行"，收敛两选一，全量同步 §5.1/§5.2/§5.4/§12/CON-R021）；⑩ Q-023 迁移矩阵补三类（列迁移/重跑规则/系统收敛）；⑪ Q-024 两级测试替身；⑫ Q-025 执行开始即写记录（startedAt/finishedAt）
- 影响：CON-R021/CON-R023/CON-R027 修订（规则索引已登记，共识版本 v1.2）；Task schema 增 assignee；Q-016~Q-025 closed
- 状态：已回写（共识 v1.2，2026-08-20）

## 2026-08-20 M2 三角色扫描 3 P0 回写 + 多 agent 拓展（共识 v1.1）

- 类型：共识修订回写（Q-013/014/015 确认 + 用户新增 agentSpec 拓展）
- 内容：① Q-013 → §7.1 增 `executionStatus`（8 态）+ `currentExecutionId`，执行生命周期与 columnId 双轨解耦（§5.1 补语义）；② Q-014 → §7.1 增 `dependencies: string[]`（子任务自声明，空=无依赖可并行，父任务不并行，P2 加环检测）；③ Q-015 → execution 记录增 `selfCheck: { passed, evidence? }` 判定信号（passed=true→Verify，false/超时/异常→failed）；④ 用户新增：agentSpec 补 `subagentPolicy: 'auto'|'restricted'`，新规则 **CON-R030**（多 agent 平台派发：ExecutionProvider 可扩展、dsh 作 ACP client 可编排跨平台子 agent），§10 第三方对接补表行
- 影响：Task schema 增 3 字段、execution 记录增 selfCheck、规则新增 CON-R030（规则索引已登记）；Q-013/014/015 closed
- 状态：已回写（共识 v1.1，2026-08-20）

## 2026-08-20 文档命名规则决策（ai-workflow-skills 更新对齐）

- 类型：流程规则决策（对齐 ai-workflow-skills 新增"子需求编号约定（跨期）" + prd_slug 规则）
- 内容：① **M2 起用新规则**——子需求编号用看板期独立段 `B1-B5`（B=Board，不沿用 M1 的 S 编号段，避免跨期复用）；契约 item_id 用 `b1`（**平台前缀 `feishu-` 保留**——skills 澄清平台前缀是格式固定部分必须保留，约束只作用于 item_id）；**prd_slug 标识来源 PRD**（M2 看板 PRD slug = `m2-kanban`）——契约 `feishu-b1-m2-kanban-api-contract.md`、设计 `B1-看板-m2-kanban-design.md`、记录 `B1-m2-kanban-record.md`、原型 `2026-08-19-m2-kanban-prototype.html` ② **M1 存量保持现状**（已交付、引用链深、改名收益低），不追溯改名 ③ 原型改名：`docs/prototype/index.html` → `2026-08-14-m1-prototype.html`、`m2-kanban.html` → `2026-08-19-m2-kanban-prototype.html`（对齐 `<date>-<slug>-prototype` 规则）
- 影响：M2 拆子需求（Phase 4）时按 `B1-B5` + prd_slug 编号；M1 文档不变
- 状态：已定案（用户确认 2026-08-20）

## 2026-08-19 M2 任务看板共识基线发布（共识 v1.0）

- 类型：共识基线发布（M2 看板模块首次建立）
- 内容：从 M2 PRD v0.6 + 交互原型 v6 提取整理为业务事实源——任务执行状态机（idle→queued→running→paused/interrupted/cancelled/failed→Verify→Done）、人工拖拽语义（最高优先级不自动矫正）、执行中修订 AC 流程、父任务聚合规则、执行通道（ExecutionProvider + ACP 默认/插件备选/CLI 兜底）；登记规则 CON-R017~CON-R029（13 条，所属模块=看板）；登记未决项 U-001~U-003（agentSpec 任务级指定/多平台接入/并行依赖图，均 P2）
- 数据来源：M2 PRD v0.6（docs/prd/2026-08-19-m2-kanban-prd.md）、交互原型 v6（docs/prototype/2026-08-19-m2-kanban-prototype.html）
- 状态：已发布（PRD 审核通过 2026-08-19，用户确认）；待三角色扫描 + 评审