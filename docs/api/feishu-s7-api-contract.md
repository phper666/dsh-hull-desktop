# S7 测试与验收体系契约

## 契约信息

- 工作项：S7 测试与验收体系（飞书 dsh-hull-desktop 清单）
- 契约状态：已冻结
- 版本：v0.3（变更传播）
- 适用版本：M1（共识 v1.5）
- 最后更新：2026-08-18

## 需求与共识追踪

| 能力 | Story | 共识规则 | 验收标准 | 接口 | 状态 |
|---|---|---|---|---|---|
| 单元测试 | S7 | 贯穿 | 状态机/版本比较/残留检测 100% 覆盖 | node:test（A2：vitest 评估后否决——219 用例已覆盖 U3/U4 全场景，引入纯成本无新增覆盖价值） | 已定义 |
| 集成测试 | S7 | CON-R015 | spawn/就绪真实进程 | node:test（A2 同注记，覆盖单元+集成两层） | 已定义 |
| 坏版本注入 | S7 | CON-R015 | 探测目标可注入即触发回滚 | 配置注入 | 已定义 |
| e2e | S7 | 贯穿 | 升级全流程 | Playwright electron | 已定义 |
| 10s 验收 | S7 | CON-R015 | 总时长测量 | 计时工具 | 已定义 |

## 范围与非目标

### 范围

- node:test 单元测试：升级状态机迁移合法性、VersionCompare（prerelease）、SwapManager 残留检测恢复、SettingsStore 校验
- 集成测试：真实 spawn dsh 子进程 + 就绪判定（探测目标可注入）
- 坏版本注入（Q-010）：就绪探测目标/超时配置注入 → 验证自动回滚路径
- Playwright _electron e2e：启动→首装→升级全流程→回滚
- 10s 验收测量（Q-009：总时长，含 dsh 启动）

### 非目标

- 三平台测试矩阵（后续里程碑）
- dsh 官方自身的测试

## 测试分层

| 层 | 工具 | 覆盖 | 位置 |
|---|---|---|---|
| 单元 | node:test | 状态机/版本比较/残留检测/设置校验 | src/**/*.test.ts |
| 集成 | node:test | spawn/就绪/overlay 安装 | tests/integration |
| e2e | Playwright _electron | 启动/首装/升级/回滚/托盘 | tests/e2e |

## 关键测试场景

### 状态机（S3）

| # | 场景 | 输入 | 预期 |
|---|---|---|---|
| U3-01 | 合法迁移 | confirm→installing→swapping→verifying | 状态推进 |
| U3-02 | 非法迁移 | idle→installing（或 confirm→rollback） | 拒绝（A1：idle→rollback 归合法——S3 D7 W3 手动回滚承载） |
| U3-03 | 取消 | installing→cancel | 回 idle，staging 清理 |
| U3-04 | 验证失败 | verifying 超时 | → rollback → idle |

### 版本比较（S4）

| # | 场景 | 输入 | 预期 |
|---|---|---|---|
| U4-01 | rc 序数 | rc.6 vs rc.7 | rc.6 < rc.7 |
| U4-02 | pre vs 正式 | rc.7 vs 0.1.0 | rc.7 < 0.1.0 |
| U4-03 | 相等 | rc.6 vs rc.6 | 相等 |

### 残留检测（S3/Q-004）

| # | 场景 | 磁盘状态 | 预期 |
|---|---|---|---|
| U3-05 | 正常 | dsh 存在 | 正常启动 |
| U3-06 | 中断续替 | dsh 缺 + staging 在 | staging→dsh |
| U3-07 | 中断回滚 | dsh 缺 + previous 在 | previous→dsh |

### e2e（贯穿）

| # | 场景 | 预期 |
|---|---|---|
| E2E-01 | 冷启动 | UI 可交互，总时长记录 ≤10s（Q-009 权威口径）——注记（共识 v1.5 主窗口形态）：壳框架 + 右侧 WebContentsView 结构下「UI 可交互」的窗口结构断言更新归 e2e 实现波，本契约保持验收口径不变 |
| E2E-02 | 首装+取消 | 引导态 + 重装成功（真实 registry 依赖注记，B8） |
| E2E-03 | 升级全流程 | 版本变化 + 数据无损 |
| E2E-04 | 坏版本注入 | 自动回滚，可用 |
| E2E-05 | 退出清理 | 零残留 |
| E2E-06 | 托盘 | 打开主窗口/设置入口/升级中禁用（覆盖 T6-04/T1-06，A3）——注记（共识 v1.5）：托盘为补充入口（壳左侧导航为主），入口层级断言随 e2e 实现波更新 |
| E2E-07+ | 设置页场景组 | T6-01 registry 持久化 / T6-03 关闭即退出 / T6-05 校验提示（A3） |

## 接口清单

| # | 状态 | 接口 | 用途 |
|---|---|---|---|
| 1 | NEW | test:unit（node:test） | 单元测试 |
| 2 | NEW | test:integration | 集成测试（真实 spawn） |
| 3 | NEW | test:e2e（playwright） | e2e 全流程 |
| 4 | NEW | verify:acceptance | 10s 验收测量脚本（**不启动 electron，依赖 e2e 已产日志**——t4 是 Electron 窗口事件，须 e2e 先跑产日志，A4） |

## 开放问题

无

## 协调事项

- e2e 需要真实 dsh 网络安装（首次测试环境需 registry 可达）

## 完成记录

- 契约草案生成（2026-08-14），待评审

## 决策与踩坑

- Q-009：10s 验收 = 总时长（含 dsh 启动），测量口径明确
- Q-010：坏版本注入 = 就绪探测目标/超时配置注入，不改业务代码

## 变更记录

- 2026-08-18：变更传播（主窗口形态变更，共识 v1.5）：E2E-01/E2E-06 加注记——壳框架 + 右侧 WebContentsView 结构下窗口结构断言与入口层级断言（壳导航为主）归 e2e 实现波，本契约保持验收口径不变。升 v0.3
- 2026-08-18：dsh 版本注记——npm dist-tag latest = 0.1.0-rc.7（08-18 实测；更新 08-14 实测 rc.6 语境，来源共识 §8）。U4 用例 rc.6/rc.7 为版本比较语义示例，保持原样
- 2026-08-17：评审修订封口 6 项：适用版本升共识 v1.4 + 变更记录格式统一（A5）；U3-02 修订——idle→rollback 归合法（S3 D7 W3 手动回滚承载），非法用例换 idle→installing/confirm→rollback（A1）；vitest 裁决注记——测试框架沿用 node:test（评估后否决 vitest，覆盖单元+集成两层）（A2）；E2E 场景补全——E2E-06 托盘 + E2E-07+ 设置页场景组（A3）；verify-acceptance 表述修正——不启动 electron，依赖 e2e 已产日志（A4）；封口声明——清单外冻结项不动（A6）。升 v0.2
- 2026-08-14：新建契约（草案）

## 自检记录

- 分层测试完整；注入点明确；验收口径可执行；无 TBD
